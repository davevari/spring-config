cook:
  special: Filet Mignon
  favoriteColor: green

Configutation:
  Loggers:
      Root:
        level: info
        AppenderRef:
          - ref: CONSOLE
      Logger:
        - name: com.pearson.architecture.sandbox.Menu
          additivity: false
          level: debug
          AppenderRef:
            - ref: CONSOLE
